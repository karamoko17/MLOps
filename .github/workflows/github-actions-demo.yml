name: Demo Workflow

on:
  push:
    branches:
      - main  # Branche sur laquelle ce workflow s'exécute

jobs:
  build:
    runs-on: ubuntu-latest  # Exécute le workflow sur une machine Ubuntu

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3  # Action GitHub officielle pour récupérer le code source du dépôt

      - name: Run a Hello World script
        run: echo "Hello, GitHub Actions!"
